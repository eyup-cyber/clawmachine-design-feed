# LICENSE NOTE — `application/` (Tailwind UI)

First-pass clean-room reconstructions (30 files) authored in an earlier session before the bulk `components/` generation. Same licensing basis as `components/`.
